services:
  go:
    container_name: go-chi-lambda
    volumes:
      - ./:/app/
    tty: true
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 8080:3000
    command: sh -c 'go mod tidy && air'